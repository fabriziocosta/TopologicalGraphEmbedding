one idea is to fit simple classifiers on the longitudinal coordinates, ie find where a point is on the closest spline and classify according to that
given a performance measure we can optimize for the best parameters of splines etc 
this should also work in d dimensions rather than 2 