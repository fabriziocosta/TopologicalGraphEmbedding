[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "skeletal-embedding"
version = "0.2.0"
description = "Sparse topology-aware skeletal embeddings for noisy point clouds"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
]

[project.optional-dependencies]
notebooks = [
    "matplotlib>=3.7",
    "pandas>=2.0",
    "scikit-learn>=1.3",
    "umap-learn>=0.5.5",
    "ipywidgets>=8.0",
    "plotly>=5.18",
    "h5py>=3.8",
    "requests>=2.28",
    "ripser>=0.6",
]
dev = [
    "pytest>=8",
    "coverage>=7",
    "ruff>=0.6",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["skeletalembedding*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra"
